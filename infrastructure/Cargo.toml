[package]
name = "infrastructure"
version = "0.1.0"
edition = "2021"

[dependencies]
hickory-resolver = { version = "0.24", features = ["tokio-runtime"] }  # Enable async
jsonwebtoken = "9.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
chrono = "0.4"  # Added for timestamp handling
